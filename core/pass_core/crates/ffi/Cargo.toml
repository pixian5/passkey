[package]
name = "pass-core-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pass-merge = { path = "../merge" }
pass-csvio = { path = "../csvio" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
