{
  "name": "codex-tauri",
  "private": true,
  "version": "1.5.3",
  "type": "module",
  "scripts": {
    "prepare:dist": "node scripts/prepare-dist.mjs",
    "sync:web-ui": "node scripts/sync-web-ui.mjs",
    "dev": "npm run prepare:dist && tauri dev",
    "build": "npm run prepare:dist && tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "jsqr": "^1.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
