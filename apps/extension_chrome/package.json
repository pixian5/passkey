{
  "name": "pass-extension-chrome-shell",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome shell for the shared Pass browser extension.",
  "scripts": {
    "build": "cd ../extension_shared && npm install && npm run build",
    "build:watch": "cd ../extension_shared && npm install && npm run build:watch"
  }
}
