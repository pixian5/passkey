{
  "name": "pass-extension-firefox-shell",
  "version": "1.1.5",
  "private": true,
  "description": "Firefox shell for the shared Pass browser extension.",
  "scripts": {
    "build": "cd ../extension_shared && npm install && npm run build",
    "build:watch": "cd ../extension_shared && npm install && npm run build:watch"
  }
}
